name: my first workflow

on: push
jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:

            - name: Checkout Repo
              uses: actions/checkout@v4

            - name: List and Read file
              run: |
                 echo "my first GitHub Action Job"
                 ls -ltra
                 cat README.md
            - name: Generate ASCII Artwork
              run: cowsay -f dragon "Run for cover, I am a DRAGON...RAA" >> dragon.txt
                   
         #   - name: List file
         #    run: ls
            
         #   - name: Read file
         #     run: cat README.md

          